using System;

/*
 * @Description
 * Strings are sequences of characters
 * each character is a Unicode symbol
 * represented by the string data type in C Shrap
 * String objects contain an immutable sequence of characters
 * Strings use Unicode to support multiple languages and alphabets
 * Strings are stored in the dynamic memory 
 * System.String is reference type
 * Most Important Static Methods:
 * String.Compare; String.Concat;
 * non-static methods:
 * IndexOf(); LastIndexOf(); Substring(); Split(); Replace(); Remove(); ToLower(); ToUpper(); Trim(); 
 */
namespace _01.StringsInCSharp
{
    class Program
    {
        static void Main(string[] args)
        {
        }
    }
}
